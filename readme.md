#### OIDC Authorization Code Grant - Server-side App Demo

- Install Heroku CLI `brew install heroku`

- Clone and enter repo, run `npm install`

- Run `heroku local` to start the app on `localhost:3000`

